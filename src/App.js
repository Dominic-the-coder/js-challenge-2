import React, { useState } from 'react';

// Test data - Do not modify
const items = [
  { id: 1, name: "Milk" },
  { id: 2, name: "Bread" },
  { id: 3, name: "Eggs" },
  { id: 4, name: "Butter" },
  { id: 5, name: "Cheese" }
];

function App() {
  // Your code here
  // Hints:
  // 1. Use useState to keep track of purchased status for each item.
  // 2. Initialize purchased status for all items as false (not purchased).
  // 3. Create a function to toggle purchased status when a checkbox is clicked.
  // 4. Use .map() to render the list of items with their checkboxes.

  

  return (
    <div>
      {/* Your JSX here */}
    </div>
  );
}

export default App;